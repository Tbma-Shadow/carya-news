{
  "name": "carya-news",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend run build && node scripts/build-worker.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "fast-xml-parser": "^5.3.0",
    "linkedom": "^0.18.12",
    "@mozilla/readability": "^0.6.0"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
